server:
  port: 8011


spring:
  application:
    name: userserver
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
  profiles:
    active: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8091/eureka/